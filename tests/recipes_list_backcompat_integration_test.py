# Generated by Skyramp v1.3.27 on 2026-06-18 13:29:37.461728384 +0000 UTC m=+534.680567872
# Command: skyramp generate integration rest \
# 		--auth-type Bearer \
# 		--framework pytest \
# 		--language python \
# 		--output recipes_list_backcompat_integration_test.py \
# 		--output-dir /home/runner/work/mealie-testb/mealie-testb/tests \
# 		--overwrite \
# 		--runtime local \
# 		--trace /home/runner/work/mealie-testb/mealie-testb/tests/scenario_recipes-list-backcompat.json

# Import of required libraries
import skyramp
import os
import time


# URL for test requests
URL_localhost = skyramp.get_base_url("SKYRAMP_TEST_BASE_URL", "http://localhost:9000")

def test_integration():
    # Invocation of Skyramp Client
    client = skyramp.Client()
    # Definition of authentication header
    headers = {}
    if os.getenv("SKYRAMP_TEST_TOKEN") is not None:
        headers["Authorization"] = "Bearer " + os.getenv("SKYRAMP_TEST_TOKEN")

    # Request Body
    recipes_POST_request_body = r'''{
        "name": "SkyBackcompatRecipe"
    }'''
    
    # Execute Request
    recipes_POST_response = client.send_request(
        url=URL_localhost,
        path="/api/recipes",
        method="POST",
        body=recipes_POST_request_body,
        headers=headers
    )
    # Generated Assertions
    assert recipes_POST_response.status_code == 201

    # Execute Request
    recipes_GET_response = client.send_request(
        url=URL_localhost,
        path="/api/recipes",
        method="GET",
        headers=headers
    )
    # Generated Assertions
    assert recipes_GET_response.status_code == 200
    assert skyramp.get_response_value(recipes_GET_response, "page") >= 1
    assert skyramp.get_response_value(recipes_GET_response, "per_page") >= 1
    assert skyramp.get_response_value(recipes_GET_response, "total") >= 1
    assert skyramp.get_response_value(recipes_GET_response, "items.0") is not None


if __name__ == "__main__":
    test_integration()
